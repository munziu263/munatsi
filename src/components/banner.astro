<section class="flex h-full items-center justify-center">
  <div class="mx-auto max-w-screen-xl px-4 lg:flex lg:h-screen lg:items-center">
    <div class="mx-auto max-w-xl text-center">
      <slot name="headline" />
      <div class="hidden">
        <div class="sm:text-xl/relaxed mt-4 text-white">
          <slot name="tagline" />
        </div>
        <div class="mt-8 flex hidden flex-col justify-center gap-4 sm:block">
          <slot name="icons" />
        </div>
      </div>
    </div>
  </div>
</section>
